query FetchMaterials($limit: Int!, $offset: Int!){
    products(limit: $limit, offset: $offset){
    results {
      masterData {
        current {
          allVariants {
            attributesRaw(includeNames: ["14_material_of_conductor"]) {
              name
              value
            }
          }
        }
      }
    }
  }
}