query ProductSelectionsQuery($limit: Int = 500, $offset: Int = 0) {
  productSelections(limit: $limit, offset: $offset) {
    total
    count
    results {
      id
      name(locale: "en")
    }
  }
}